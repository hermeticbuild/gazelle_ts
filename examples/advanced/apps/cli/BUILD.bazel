load("//tools:ts.bzl", "myorg_ts_binary", "myorg_ts_library")

# Hand-written launcher. Gazelle attaches the generated package library.
myorg_ts_binary(
    name = "cli_bin",
    data = ["users.json"],
    entry_point = "main.ts",
    deps = [":cli"],
)

filegroup(
    name = "templates",
    srcs = glob(["*.template.ts"]),
)

myorg_ts_library(
    name = "cli",
    srcs = [
        "main.ts",
        "types.d.ts",
    ],
    tsconfig_types = ["node"],
    visibility = ["//visibility:public"],
    deps = [
        "//:node_modules/@myrepo_generated/synthetic",
        "//:node_modules/@types/node",
        "//packages/core",
        "//packages/utils/math/deep",
        "//tools:mystery",
    ],
)
