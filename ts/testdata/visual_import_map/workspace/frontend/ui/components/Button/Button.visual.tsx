import { buttonLabel } from "./Button";
import type { ComponentVisualModule } from "#workspace/frontend/testing/component-vrt/componentVisual.js";

export const buttonVisualModule: ComponentVisualModule = {
  id: buttonLabel(),
};
