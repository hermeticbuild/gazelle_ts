import { buttonLabel } from "./Button";
import type { ComponentVisualModule } from "#pplx/frontend/testing/component-vrt/componentVisual.js";

export const buttonVisualModule: ComponentVisualModule = {
  id: buttonLabel(),
};
